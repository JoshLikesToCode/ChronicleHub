apiVersion: v1
kind: Service
metadata:
  name: {{ include "chroniclehub.fullname" . }}
  labels:
    {{- include "chroniclehub.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
  - port: {{ .Values.service.port }}
    targetPort: http
    protocol: TCP
    name: http
  selector:
    {{- include "chroniclehub.selectorLabels" . | nindent 4 }}
