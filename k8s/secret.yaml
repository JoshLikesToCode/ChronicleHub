# WARNING: DO NOT COMMIT REAL SECRETS TO VERSION CONTROL
# This is a placeholder for local development only.
# In production, use tools like:
# - Sealed Secrets
# - External Secrets Operator
# - Vault
# - Cloud provider secret managers (AWS Secrets Manager, Azure Key Vault, GCP Secret Manager)

apiVersion: v1
kind: Secret
metadata:
  name: chroniclehub-secret
  labels:
    app: chroniclehub
type: Opaque
data:
  # Base64 encoded API key for local development
  # Actual value: "local-dev-key-12345"
  # To generate: echo -n "your-api-key" | base64
  api-key: bG9jYWwtZGV2LWtleS0xMjM0NQ==
